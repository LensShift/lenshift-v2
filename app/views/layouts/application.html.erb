<!DOCTYPE html>
<html lang="en">
  <head>
    <%= google_tag_manager_script_tag(:lensshift) %>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Resource Hub for Responsible Social Impact - LensShift" %></title>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= stylesheet_link_tag "application" %>
    <%= include_gon %>
    <%= javascript_pack_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body infinite-wrapper>
    <%= google_tag_manager_noscript_tag(:lensshift) %>
   
    <div id="app">
       <%= render partial: "layouts/navbar" %>
       <div class='grid-container fluid' id='main-container'>
          <% if notice.present? || flash[:notice] %>
          <p class="callout primary" data-closable><%= notice %></p>
          <% end %>

          <% if alert.present? || flash[:error] %>
          <div class="callout alert" data-closable><%= alert || flash[:error] %></div>
        <% end %>

        <%= yield %>
       
       </div>
       <div class='push'></div>
       <%= render partial: 'layouts/footer' %>
    </div>
    

  </body>
</html>
