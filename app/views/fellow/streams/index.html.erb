<p id="notice"><%= notice %></p>

<h1>Streams</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @streams.each do |stream| %>
      <tr>
        <td><%= stream.title %></td>
        <td><%= link_to 'Show', fellow_stream_path(stream) %></td>
        <td><%= link_to 'Edit', edit_fellow_stream_path(stream) %></td>
        <td><%= delete_button_to 'Delete',  fellow_stream_path(stream), class: 'button table-button alert' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fellow Stream', new_fellow_stream_path %>
