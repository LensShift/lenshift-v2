<div class='row align-center'>

<div class="column small-12 medium-12 large-12">

  <h1>LensShifters</h1>

  <table>
    <thead>
      <tr>
        <th>id</th>
        <th>User</th>
        <th>Signed up date</th>
        <th>Confirmed date</th>
        <th>Last signed in</th>
        <th>Admin?</th>
      </tr>
    </thead>

    <tbody>
      <% @lens_shifters.each do |user| %>
        <tr>
          
          <td><%= user.id %></td>
          <td><%= link_to fellow_lens_shifter_path(user.id) do %><%= user.full_name %><% end %></td>
          <td><%= user.created_at.strftime("%m-%d-%Y") %></td>
          <td><%= user.confirmed_at.strftime("%m-%d-%Y") if !user.confirmed_at.nil? %></td>
          <td><%= user.last_sign_in_at.strftime("%m-%d-%Y") if !user.last_sign_in_at.nil? %></td>
          <td><%= user.admin? ? 'admin' : '' %></td>
          
        </tr>
        <% end %>
    </tbody>
  </table>
  <br />
</div>
</div>